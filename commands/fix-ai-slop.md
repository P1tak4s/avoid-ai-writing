Audit the following text for AI writing patterns and rewrite it cleanly. Use the avoid-ai-writing skill.

Detect the language automatically (English, Lithuanian, or mixed). Load the matching reference file from the skill.

Return exactly four sections:
1. **Issues found** — quote every AI-ism with its category label
2. **Rewritten** — the clean version
3. **What changed** — one-line summary of edits
4. **Second pass** — confirm clean or flag survivors

Text to fix:
$ARGUMENTS
